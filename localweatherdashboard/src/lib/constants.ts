/** Milliseconds to wait for the Open-Meteo API before aborting the request. */
export const FETCH_TIMEOUT_MS = 10_000

/** Number of upcoming hourly forecast entries to slice out of Open-Meteo's full-week hourly array. */
export const HOURLY_FORECAST_COUNT = 8
